
import {
    USER_START,
    USER_SUCCESS,
    USER_CREATE_START,
    USER_CREATE_SUCCESS
} from 'actionTypes'


const initialState = {}

export default (state = initialState, {type, payload}) => {
    console.log('type');
    console.log(type);
    switch (type) {
        case USER_START:
            console.log('payload = USER_START' );
            console.log( - state);
            return state;
        case USER_SUCCESS:
            console.log('payload = USER_SUCCESS' );
            console.log( - payload);
            return payload;



        case USER_CREATE_START:
            console.log('payload = USER_SUCCESS' );
            console.log( - payload);
            return state;
        case USER_CREATE_SUCCESS:
            console.log('payload = USER_SUCCESS' );
            console.log( - payload);
            return state;
        default:
            return state
    }
}